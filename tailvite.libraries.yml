# Use the source path instead of destination. "scss/styles.scss" instead of "dist/css".
# Paths will be altered for local or prod with hook_library_info_alter().
global:
  js:
    resources/main.js: { attributes: { type: 'module' } }
  dependencies:
    - core/jquery
    - core/drupalSettings
